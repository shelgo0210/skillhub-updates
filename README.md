# SkillHub signed update feed

This public repository contains only signed SkillHub update metadata and payloads. It contains no source code, signing private key, GitHub token, or user data.

- Transport: HTTPS/TLS
- Authenticity: ECDSA P-256 manifest signatures
- Integrity: SHA-256 payload hashes
- Source code: kept in the separate private repository
